# MNML — مرور نگارشی

MNML یک زبان نشانه‌گذاری سبک با سینتکس پایتونی است. هدر نیازی به برچسب باز و بسته ندارید — فقط تگ، آرگومان و محتوا.

---

## ساختار کلی

```
tag: محتوا
```

```
tag(arg1, arg2): محتوا
```

هر چیزی که indent (فاصله از چپ) بیشتری داشته باشه، **فرزند** تگ قبلی محسوب میشه.

---

## تگ‌ها

### متن

```
title: عنوان صفحه

h1: عنوان اصلی
h2: زیرعنوان
h3: سطح ۳
p: یک پاراگراف معمولی

bold: متن ضخیم
italic: متن کج
code: کد یا متن مونواسپیس
pre:
    کد چند خطی
    دومین خط
```

### لیست‌ها

لیست بدون ترتیب:
```
ul:
    - آیتم اول
    - آیتم دوم
    - آیتم سوم
```

لیست مرتب:
```
ol:
    - اول
    - دوم
    - سوم
```

### لینک و تصویر

```
link("https://example.com"): کلیک کنید

image("photo.png")
```

### خط جداکننده

```
hr:
```

### جدول

```
table:
    row:
        cell: نام
        cell: سن
        cell: شهر
    row:
        cell: علی
        cell: ۲۵
        cell: تهران
    row:
        cell: سارا
        cell: ۲۲
        cell: اصفهان
```

### ویدیو و صدا

```
video("myvideo.mp4")

audio("mysong.mp3")
```

### فرم

```
form("mynet://localhost:7443/submit"):
    input(name="username", placeholder="نام کاربری")
    input(name="password", type="password", placeholder="رمز")
    button: ارسال
```

### دکمه

```
button: کلیک من
```

---

## ورودی‌ها (Variables)

```
user_name: علی

h1: سلام {user_name}
```

---

## شرط

```
user_role: admin

if user_role == "admin":
    p: شما ادمین هستید!
else:
    p: شما کاربر عادی هستید.
```

تا حالا if/else ساپورت شده در MNML.

---

## حلقه

```
items:
    - سیب
    - پرتقال
    - موز

ul:
    for item in items:
        - {item}
```

---

## ترکیب چند تگ

```
title: صفحه ترکیبی

h1: خوش آمدید

p: این یک پاراگراف هست.

link("mynet://localhost:7443/about"): درباره ما

hr:

ul:
    - خبر اول
    - خبر دوم

video("demo.mp4")
```

---

## کامنت

خطی هر کدومی که با `#` شروع بشه کامنت محسوب میشه:

```
# این یک کامنت هست
h1: صفحه من
# این هم کامنت
```

---

## ساینتکس‌های خیلی کوتاه

```
# بدون دو نقطه برای تگ‌های ساده
h1 سلام دنیا
p این یک پاراگراف
```

---

## نکات

- هر تگ بیشتر از یک آرگومان می‌تونه داشته باشه:
  ```
  image("pic.png", alt="یک تصویر", width="300")
  ```
- رشته‌ها با `".."` یا `'..'` کوتاه بشن
- فاصله بین تگ و محتوا اجباری نیست
- هر خط فقط یه تگ داره (یا ادامه همان تگ)
